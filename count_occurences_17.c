#include<stdio.h>
void main()
{
    char str[50];
    int i, spaces = 0, digits = 0, other = 0;
    printf("Enter the string:");
    fgets(str,sizeof(str),stdin);
    printf("\nInput String: %s\n", str);
    
    for(i = 0;str[i]!='\0'; i++)
    {
        switch(str[i])
        {
            case


    


}
