set number " Add numbers to each line on the left-hand side.
syntax on " Turn syntax highlighting on
set shiftwidth=4 "sets the width of a tab to 4 spaces
set tabstop=4 "sets the number of spaces to be used for each step of auto-indentation
set smartindent
set mouse=a
